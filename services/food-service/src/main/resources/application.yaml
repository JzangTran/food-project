server:
  port: 8082

spring:
  application:
    name: food-service
  datasource:
    url: jdbc:postgresql://14.225.254.174:5555/food_ordering
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect